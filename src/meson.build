sources = [
  'main.vala'
]

initial_file = 'initial.vala'

run_command('cp', initial_file, meson.current_build_dir())

executable('optimus-prime',
  sources,
  dependencies: dependencies,
  install: true)
